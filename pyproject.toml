[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "flowcept"
version = "0.5.3"
requires-python = ">=3.10"
dependencies = ["pandas"]

[project.optional-dependencies]
dev = ["ruff"]
